import React, { Component } from "react"

class Welcome extends Component {
    render() {
        return <h1>class components {this.props.name} and  {this.props.nickName}</h1>
    }
}

export default Welcome